# Dashboard media

The main README already points to these files. Upload the finished media here using the exact names below:

- `analyze-report.png` — completed single-report analysis
- `batch-review.gif` — upload, filter and inspect the batch queue
- `confidence-routing.gif` — change the confidence requirement and show the routing decision update

Keep the GIFs short and crop browser chrome before exporting them. Replacing a file with a newer capture does not require another README edit.
